server:
  port: 9090
spring:
  application:
    name: League-Application
  datasource:
    url: jdbc:mysql://localhost:3306/leagueApplication
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
